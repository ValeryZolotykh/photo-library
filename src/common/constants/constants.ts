// Minimum and maximum values for the random delay range (im ms)
export const MIN_DELAY = 200;
export const MAX_DELAY = 300;

// Key for storing photos in local storage
export const LOCAL_STORAGE_KEY = "photoUrls"; 